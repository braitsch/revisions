<?xml version="1.0" encoding="utf-8"?>
<application xmlns="http://ns.adobe.com/air/application/3.1" minimumPatchLevel="0">

    <!-- The application identifier string, unique to this application. Required. -->
    <id>com.braitsch.revisions</id>

    <!-- Used as the filename for the application. Required. -->
    <filename>Revisions</filename>

    <!-- The name that is displayed in the AIR application installer.
         May have multiple values for each language. See samples or xsd schema file. Optional. -->
    <name>
    	<text xml:lang="en">Revisions</text>
    </name>

    <!-- An application version designator (such as "v1", "2.5", or "Alpha 1"). Required. -->
	<versionNumber>0.5.74</versionNumber>
	<versionLabel>0.5.74</versionLabel>

    <!-- Description, displayed in the AIR application installer.
         May have multiple values for each language. See samples or xsd schema file. Optional. -->
    <description>
    	<text xml:lang="en"></text>
    </description>

    <!-- Copyright information. Optional -->
    <copyright>© Stephen Braitsch 2011</copyright>

    <!-- Must include extendedDesktop for Revisions support -->
	<supportedProfiles>extendedDesktop desktop</supportedProfiles>	

    <!-- Settings for the application's initial window. Required. -->
    <initialWindow>
		<content>Revisions.swf</content>
        <title>Simple File Versioning and Sharing</title>
        <x>50</x>
        <y>50</y>
        <width>800</width>
        <height>600</height>
        <minSize>800 600</minSize>
        <visible>false</visible>
        <resizable>true</resizable>
        <minimizable>true</minimizable>
        <maximizable>true</maximizable>
        <transparent>false</transparent>
        <systemChrome>standard</systemChrome>
    </initialWindow>

    <!-- The subpath of the standard default installation location to use. Optional. -->
    <!-- <installFolder>../Users/braitsch/Desktop</installFolder> -->

    <!-- The subpath of the Programs menu to use. (Ignored on operating systems without a Programs menu.) Optional. -->
    <!-- <programMenuFolder></programMenuFolder> -->

    <!-- The icon the system uses for the application. For at least one resolution,
         specify the path to a PNG file included in the AIR package. Optional. -->
    <icon>
        <image48x48>icons/RevisionsIcon48.png</image48x48>
        <image128x128>icons/RevisionsIcon128.png</image128x128>
    </icon>

    <!-- Whether the application handles the update when a user double-clicks an update version
    of the AIR file (true), or the default AIR application installer handles the update (false).
    Optional. Default false. -->
    <!-- <customUpdateUI></customUpdateUI> -->
   
    <!-- Whether the application can be launched when the user clicks a link in a web browser.
    Optional. Default false. -->
    <!-- <allowBrowserInvocation></allowBrowserInvocation> -->

    <!-- Listing of file types for which the application can register. Optional. -->
    <!-- <fileTypes> -->

        <!-- Defines one file type. Optional. -->
        <!-- <fileType> -->

            <!-- The name that the system displays for the registered file type. Required. -->
            <!-- <name></name> -->

            <!-- The extension to register. Required. -->
            <!-- <extension></extension> -->
           
            <!-- The description of the file type. Optional. -->
            <!-- <description></description> -->
           
            <!-- The MIME content type. -->
            <!-- <contentType></contentType> -->
           
            <!-- The icon to display for the file type. Optional. -->
            <!-- <icon>
                <image16x16></image16x16>
                <image32x32></image32x32>
                <image48x48></image48x48>
                <image128x128></image128x128>
            </icon> -->
           
        <!-- </fileType> -->
    <!-- </fileTypes> -->

</application>